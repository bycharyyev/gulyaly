# ⚡ Быстрая проверка и деплой VPS

## 🔍 Проверка текущего состояния

Выполните на VPS:

```bash
ssh root@89.104.74.7
bash scripts/check-vps.sh
```

Или скопируйте скрипт и запустите:

```bash
# С вашего компьютера
scp scripts/check-vps.sh root@89.104.74.7:/tmp/

# На VPS
ssh root@89.104.74.7
bash /tmp/check-vps.sh
```

## 🚀 Быстрый деплой

Если проект уже настроен, просто обновите:

```bash
ssh root@89.104.74.7
cd /var/www/gulyaly
bash scripts/deploy.sh
```

## 🆕 Первый деплой

Если проект еще не развернут:

```bash
ssh root@89.104.74.7

# 1. Полная настройка VPS (если нужно)
bash /tmp/setup-vps-complete.sh

# 2. Клонировать проект
mkdir -p /var/www/gulyaly
cd /var/www/gulyaly
git clone https://github.com/bycharyyev/gulyaly.git .

# 3. Настроить окружение
bash scripts/vps-setup.sh

# 4. Готово! Приложение должно работать на https://gulyaly.com
```

## 📋 Чек-лист

- [ ] Node.js 20 установлен
- [ ] PM2 установлен и запущен
- [ ] PostgreSQL установлен и настроен
- [ ] Nginx установлен и настроен
- [ ] Проект клонирован в `/var/www/gulyaly`
- [ ] `.env` файл создан и настроен
- [ ] База данных создана и миграции применены
- [ ] Приложение собрано (`npm run build`)
- [ ] PM2 процесс запущен
- [ ] Nginx проксирует на `localhost:3000`
- [ ] Домен `gulyaly.com` указывает на IP
- [ ] SSL сертификат установлен (опционально)

Подробная инструкция в файле `DEPLOY.md`

